# Step Notes for q006

- [2026-07-12 18:25:05] Built the q006 SQL artifact as the simplest possible CE trend check: one true national vacancy-rate series from the `us` row in `gold.housing_core_wide`, annual values only, 2015 through 2024, and no peer comparison layer.
- [2026-07-12 18:25:05] The SQL result looks analytically clean before charting: 10 rows, one row per year, vacancy rate declines from `12.3` in 2015 to `10.1` in 2024, and the visible downshift begins after 2019.
- [2026-07-12 18:25:05] The first Python render attempt failed for environment reasons rather than chart logic: shell `python3` could not import the editable `chart_engine` package or `vl-convert`. Rerunning the wrapper with `.venv312/bin/python` fixed the intended package path and export dependency.
- [2026-07-12 18:25:05] The next Python render surfaced two real parity gaps. First, the packaged theme still resolved to `Inter`, which caused the static PNG to drop most text on this machine until the wrapper forced `Arial`. Second, using Python `NumberFormat(unit=\"percent\")` on already-percent-scaled values produced a `1300%`-style y-axis. For this run I switched the wrapper to numeric formatting and logged the semantic mismatch as a Python formatting issue rather than changing the data.
- [2026-07-12 18:25:05] The R reference path rendered cleanly from the same `result.csv` without any SQL-shape patches. That is a good sign that the q003 `source` / `vintage` lesson carried over into the trend workflow correctly.
- [2026-07-12 18:25:05] Side-by-side parity verdict for q006: `match_with_minor_drift`. The trend shape, time window, and headline interpretation match between R and Python. The remaining drift is presentational: Python still needs a better default font fallback for static export, it shows an unnecessary one-series legend, and its line-chart percent semantics are ambiguous when the input is already in percentage points.
- [2026-07-12 18:25:05] Wrote a canonical `post.md`, updated the cross-run manual log, and q006 is ready to count as `ran` in the backlog because parity is not blocked.
