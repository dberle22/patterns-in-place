# Position Trajectory Outputs

This folder's future artifacts are QA bundles from a reusable parameterized
analysis.

Planned structure:

- `figures/<cbsa_code>/`
  Lightweight HTML checks written by `trajectory.py`.

The Marimo notebook renders exploratory outputs in place and does not need to
export files. Do not treat these QA artifacts, or the Time-Series engine's
review snapshots, as locked issue deliverables or downstream data sources.
