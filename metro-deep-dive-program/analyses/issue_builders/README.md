# Issue Builders

`issue_builders/` holds reusable act-level notebook builders.

These are not market issues themselves. They are reusable packaging layers that
turn shared analysis outputs into standard issue-ready assets.

Examples:

- a reusable `Act 1` identity builder
- a reusable `Act 3` dynamics builder

Market-specific issue folders decide what to actually pull through.
