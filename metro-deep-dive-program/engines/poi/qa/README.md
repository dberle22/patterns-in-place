# POI QA Outputs

This directory will hold reproducible review queries, reports, and
map-ready samples after the corresponding build epics open. DuckDB products
remain canonical; display artifacts are review aids only.

Each source run must eventually report source row accounting, coordinate and
identity validity, mapping coverage, likely duplicates, and tract/market
assignment coverage as specified in [the contract](../CONTRACT.md).
